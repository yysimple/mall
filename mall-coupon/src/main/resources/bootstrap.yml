spring:
  application:
    name: mall-coupon
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        # 配置中心的地址
        server-addr: localhost:8848
        # 指定配置文件格式，相当于gitee上的那个文件
        file-extension: yaml
        # 分组
        group: DEFAULT_GROUP
        # 命名空间
        namespace: bf352b96-e3e6-406d-addb-cc4b903a5660